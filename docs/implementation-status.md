# Implementation status

This ledger records what this repository implements and what its automated
evidence can support.

## Live plugin integration

| ID | Requirement | Implementation | Evidence | Status |
| --- | --- | --- | --- | --- |
| INTEGRATION-001 | In an isolated private PipeWireAO core, publish one complete FITS image directly into the generic discard sink and clean up every fixture-owned object and process. | Maintained `spa-fits-discard-live` fixture creates an exact FITS source -> discard link through public native-protocol factories in a unique runtime and core. | The live test reaches an Active link, observes exactly one buffer, one data block, and 24 bytes with no discard protocol error, then observes removal of both nodes and the link before terminating and reaping the private core. | Functionally verified for one 4 by 3 U16 FITS ndarray image; no latency or broader live-format claim |

## Generic discard sink

| ID | Requirement | Implementation | Evidence | Status |
| --- | --- | --- | --- | --- |
| DISCARD-001 | Expose one ordinary non-actuating SPA sink that accepts any fixated SPA format. | `api.pipewireao.discard` with one input port and format-preserving negotiation | DSO test negotiates audio, video, and ndarray formats, including a mandatory producer property, and rejects an unresolved choice. | Verified synthetically |
| DISCARD-002 | Return buffers without depending on their media type, block layout, memory type, mapping, or payload contents. | Standard `SPA_IO_Buffers` follower; processing reads descriptors and chunk sizes but never payload memory. | DSO test returns a zero-block buffer and an unmapped two-block DMA-BUF/MemId buffer; source review confirms no payload access. | Verified synthetically |
| DISCARD-003 | Expose bounded cumulative metrics without making observation part of processing progress. | Lock-free saturating counters enumerated as read-only `SPA_PARAM_Props`; processing emits no metric events. | DSO test verifies buffer, block, advertised-byte, protocol-error, and process-call counters across processing and restart. | Verified synthetically |
| DISCARD-004 | Pause, restart, suspend, and destroy without a driver, queue, background work, or hardware action. | Direct follower lifecycle plus the ordinary `SPA_IO_Position` scheduling handshake in `sink.c` | DSO test covers readiness admission, pause/start, suspend, resource withdrawal, and destruction. The FITS integration test covers first live graph admission and owned-object teardown. | Synthetic lifecycle verified; live pause/restart remains open |

## Bounded queue module

| ID | Requirement | Implementation | Evidence | Status |
| --- | --- | --- | --- | --- |
| QUEUE-001 | Expose an ordinary complete-buffer ndarray or raw-video boundary. | `libpipewire-module-queue` creates persistent standard input/output `pw_stream` nodes and publishes the exact `application/ndarray` or `video/raw` format only after input negotiation. | In-process tests observe both nodes before producer linking, then transfer exact ndarray and raw-video formats through both storage modes. | Verified for tested ndarray and raw-video profiles |
| QUEUE-002 | Bound capacity and require `N + 2` input-pool admission. | Fixed-capacity aligned rings, fixed 64-slot state, and capture-pool validation. | Parser boundaries 0/1/62/63, ring boundary tests, and live capacity-one negotiation with three capture buffers; other live capacities remain open. | Engine verified; live capacity one verified |
| QUEUE-003 | Apply the selected backpressure, drop-oldest, or drop-newest overflow policy while preserving retained FIFO order. | Central bounded admission operation and reverse-loop backpressure recovery. | Deterministic engine tests, concurrent replacement stress, and all three policies in both live storage modes. | Verified for capacity one; larger live capacities open |
| QUEUE-004 | Provide explicit copy and lease storage without fallback. | Output-side copy and MemFd/DmaBuf descriptor-alias paths; each lease duplicates the backing descriptor only while PipeWire installs its output pool and leaves downstream mapping independent of the input mapping pointer. | Tests cover multi-block mutation isolation, negative admission, shared backing identity, independent remapping, retained MemFd validity after input-pool withdrawal, and native-protocol replacement with a different generation inode. | MemFd verified; DmaBuf synchronization profile remains open |
| QUEUE-005 | Keep the warmed producer path bounded, allocation-free, payload-copy-free, and free of direct fd syscalls. | Fixed scans, preallocated rings/slots, input-owned counters, and no producer-path allocator/copy/fd call. | Source review and fixed iteration bounds. An ad hoc `strace -ff -k` run found no syscall stack through `capture_process`; an automated syscall gate and allocator interposition remain open. Regular PipeWire scheduler eventfd operations remain outside this claim. | Source reviewed; syscall path sampled |
| QUEUE-006 | Preserve exact format, metadata, chunks, payload, and visible sequence gaps. | Exact format POD forwarding and shared transfer helper; transport-owned Busy metadata remains link-local. | Multi-block Header/Acquisition/chunk tests plus live payload, Header, and overflow-sequence checks. | Verified for tested metadata profile |
| QUEUE-007 | Recover every lease across pause, removal, failure, and destruction. | Input and output pool state is separate; an atomically gated playback-to-capture lifecycle handoff detaches old input tokens without closing output-owned descriptors, and immutable installed output pools are replaced before a new generation becomes ready. | Tests cover destruction occupancy, transparent format/pool restart, a retained MemFd lease validated after input withdrawal, and reconnect-before-return pool revocation in both self-connected and native-protocol graphs. A direct capture-pause transition and DmaBuf revocation remain open. | Partial; MemFd pool withdrawal and generation replacement verified |
| QUEUE-008 | Expose monotonic publication, overload, completion, pool, and error counters. | Cache-line-separated counters plus pending/completion depth, active output, pool cardinality, format, configuration, ownership-transition, and requested/installed generation state are mirrored onto module and node properties. | Initial-property test and live event histories for every overflow/storage permutation. | Verified for tested live histories |
| QUEUE-009 | Bound observer retention under drop policies and isolate copy-mode downstream leases. | `N` pending slots, at most one in-flight output, `N + 2` capture admission, and output-side copy completion. | A live observer retains its delivered buffer while nine producer publications exercise both drop policies and storage modes. | Verified for capacity-one MemFd profile |
| QUEUE-010 | Separate default node groups and limit isolation claims to qualified deployment evidence. | Distinct capture/playback node and link groups. | In-process test drives the two graph components independently; physical loop/CPU placement and fixed-arrival qualification remain open. | Graph separation verified; deployment open |
| QUEUE-011 | Preserve endpoint identities and compatible downstream links across input stop, pool withdrawal, renegotiation, and restart. | Both streams are created at module initialization; exact output parameters are updated in place, and readiness waits for a complete generation-specific output pool. | A real-daemon lifecycle test observes both initial endpoints and ports with one queue identity, transfers MemFd data, retains the output endpoint/link across input-pool replacement, and validates the replacement generation's distinct inode. In-process tests also cover observer-first linking. Daemon-process restart remains open. | Remote MemFd pool restart verified; daemon-process restart open |
| QUEUE-012 | Contain link and negotiation failures without destroying the queue while retaining fail-fast handling for ownership faults. | Core loss and an unconnected input endpoint remain terminal; input/output stream errors are retained for inspection without scheduling module destruction; ownership faults stop publication and set both streams to error. | Observer removal/reconnect is covered in both storage modes. Explicit incompatible-format recovery and injected ownership-failure tests remain open. | Partial |

| ID | Requirement | Implementation | Evidence | Status |
| --- | --- | --- | --- | --- |
| ALPAO-001 | Expose one ordinary SPA sink factory. | `plugin.c`, `sink.c` | Production and mock factory tests load and enumerate `api.alpao.sink`. | Verified |
| ALPAO-002 | Require exact schema, structural format, actuator count, and profile before start. | `validate_format()` | Mock test rejects unequal schema and profile and accepts the advertised format. | Verified |
| ALPAO-003 | Consume one ordinary scheduled input without a private queue. | Standard `SPA_IO_Buffers` input in `sink.c` | Mock test submits, processes, and reclaims fixed-pool buffers. | Verified |
| ALPAO-004 | Reject malformed or non-normalized commands before the backend and return their leases. | `process_command()` and `process()` | Mock test rejects `1.5` with `-ERANGE` and reclaims the same buffer. | Verified |
| ALPAO-005 | Make proprietary SDK integration optional. | `alpao-sdk` Meson feature | SDK-disabled build and test contain no `libasdk` dependency. | Verified |
| ALPAO-006 | Open ASDK only on start, verify `NbOfActuator`, reset, send, reset, and release. | `backend-asdk.c` | ASDK simulator test with synthetic `SIM001` configuration. | Simulator verified |
| ALPAO-007 | Do not redistribute SDK or mirror configuration. | Build options and generated test fixture | Repository inspection and ignored build trees. | Verified for current tree |
| ALPAO-008 | Keep the repeated wrapper bounded and allocation-free. | Fixed pool, fixed scan, direct payload validation | Source review; no allocator or logging call in `process()` or `process_command()`. ASDK internals are not qualified. | Wrapper reviewed |
| ALPAO-009 | Provide a safe physical-device lifecycle and strict RTC qualification. | Reset lifecycle is implemented. | Requires connected-device failure and timing tests. | Not verified |
| ALPAO-010 | Define canonical profile serialization and fingerprints. | Exact opaque matching only. | Requires normative manifest and byte-stable vectors. | Open |
| ALPAO-011 | Allow a deployment to set the DEv7 `daqFreq` interface conversion rate without advertising it as command cadence. | Optional `api.alpao.daq-frequency` startup property and `asdkSet("daqFreq", ...)` | Mock contract test covers accepted and out-of-range configuration. The ASDK simulator does not implement `daqFreq`; connected-interface verification remains required. | Implemented; hardware not verified |
| ALPAO-012 | Qualify host latency through the ASDK command-packing boundary without requiring a network or physical mirror. | Optional 468-actuator capture-interface integration test and benchmark | Correlates SPA process timestamps with sequenced `AITC` capture timestamps, checks frame size/order/loss, and supports closed-loop, fixed-rate, burst, affinity, memory locking, and `SCHED_FIFO` runs. | Host harness implemented; controlled-host results required |
| ALPAO-013 | Keep physical-to-normalized command conversion in an ALPAO-owned explicit graph operator. | ABI-v8 FGN `command-normalization-f32-f64` operator with exact demanded and normalized schemas, actuator extent, rate, scale, and per-port profile. | Direct DSO test verifies ABI admission, formats, metadata, boundary values, range failure, and malformed construction rejection. | Verified for the direct FGN boundary; scheduled graph-to-sink integration remains open |

The ASDK simulator uses a synthetic eight-actuator binary configuration created
by `alpao-binary-config` and the ASDK `sim` interface. It discards commands and
therefore cannot support claims about electronics, actuator order, physical
motion, command latency, or timing bounds.

With ASDK 4.01.12, LeakSanitizer reports the same 1,200-byte, three-allocation
retention for both the simulator test and the capture-interface test.
AddressSanitizer and UndefinedBehaviorSanitizer complete the capture test when
leak detection is disabled. This evidence does not attribute the retained SDK
state to the capture benchmark, and it does not qualify ASDK as leak-free.

## Camera sources

| ID | Requirement | Implementation | Evidence | Status |
| --- | --- | --- | --- | --- |
| CAMERA-001 | Build vendor camera sources outside the PipeWireAO fork using only public SPA headers. | `spa/plugins/egrabber`, `spa/plugins/bgapi2`, and optional Meson SDK features | Clean out-of-tree debug-optimized build through `libspa-ao-0.2`; no source include overrides. | Verified |
| CAMERA-002 | Publish complete eGrabber frames through ordinary buffers and fixed row quanta through complete ndarray buffers. | Polled graph-driver source; direct camera pool in frame mode and private camera pool plus bounded row copy in row-block mode. | Unit/factory/device/capture tests pass with the connected Euresys Gigelink camera; fixed row-quantum option validation passes. | Complete Gigelink verified; Grablink/Coaxlink row readout open |
| CAMERA-003 | Preserve BGAPI2 complete-frame behavior with both qualified producers, expose startup device discovery, and match the eGrabber GenICam control surface. | BGAPI2 manager, per-camera device, source, callback/SPSC camera adapter, and shared GenICam payload-layout policy | Factory, manager/device chain, adapter, and ten-frame source tests pass through Euresys Gigelink and Baumer BGAPI2 2.16.1 CTIs. Tests cover category/visibility metadata, enum labels, live scalar and ROI writes, stopped layout changes, pause/restart, and both Euresys readiness profiles. Startup discovery obtains deferred Euresys serial metadata through a bounded read-only open/close and preserves exact transport coordinates if enrichment is denied. | Verified |
| CAMERA-004 | Preserve daemon loading without duplicating factories between repositories. | Factory names and install paths remain `egrabber/libspa-egrabber` and `bgapi2/libspa-bgapi2`; PipeWireAO retains loader mappings only. | An isolated daemon loads the out-of-tree eGrabber DSO and performs ordinary stream capture and active teardown. | Verified |
| CAMERA-005 | Quiesce a polled camera source before announced buffers are dismantled. | Pause, Suspend, final link removal, and destruction remove the polling source before stopping the SDK and recycling the pool. | Connected camera capture covers pause, format removal, pool reuse, layout control, and teardown. | Verified for complete Gigelink mode |
| CAMERA-006 | Expose attached Camera Link camera controls and distinguish camera identity from the selected Grablink endpoint. | Optional Grablink serial transport, CLProtocol 1.x provider loader, GenApi C node map, and propagated `api.egrabber.camera-serial` constraint | The CLProtocol host/provider test covers probe, node-map access, serial mismatch, writes, and construction-failure cleanup. The full 22-test CLProtocol-enabled suite passes with connected Gigelink capture. | Implemented; Grablink hardware not verified |
| CAMERA-007 | Keep the lower-performing Aravis comparison outside the PipeWireAO core and supported camera set. | Experimental `api.aravis.source`, disabled by default, with selectable GenTL or native GigE Vision streams and native-only copied row-block output. | The opt-in plugin and factory test build against Aravis 0.10; isolated fake-GV tests cover complete-frame and progressive row-block capture. Prior Euresys Gigelink GenTL measurements remain comparison evidence, not native-GV performance or RTC qualification. | Experimental; native-GV simulator verified |
| CAMERA-008 | Use regular dependency scheduling with a selectable source readiness profile. | FITS selects `poll` or `timerfd`; BGAPI2 selects `poll` or `eventfd`; eGrabber and Aravis retain required polling; ALPAO is a normal follower. | FITS loop-driven timerfd and polling cases, BGAPI2 Euresys hardware eventfd capture, invalid-value and missing-loop rejection, core polled-driver cycle, and real daemon/client mixed-wake activation tests. | Verified functionally; deployment latency qualification open |
| CAMERA-009 | Provide a direct Andor SDK3 complete-frame source with typed camera controls and caller-owned buffers. | `api.andor3.source`, direct `AT_QueueBuffer`, probed SDK3 feature catalog, and polling source lifecycle. | Mock factory/source tests plus vendor `SIMCAM CMOS` feature, buffer, acquisition, flush, and revoke test using SDK3 3.12.30012.0. | Simulator verified; physical camera and RTC qualification open |
| CAMERA-010 | Reject source buffer pools that omit metadata required during publication. | FITS requires Header; BGAPI2 and eGrabber require Header and Acquisition; direct eGrabber DMA-BUF additionally retains its SyncTimeline requirement. | Negative pool-admission tests cover missing required metadata; FITS passes under sanitizers and connected BGAPI2/eGrabber capture tests pass with their SDKs. | Verified for the tested source profiles |
| CAMERA-011 | Make layout-changing controls failure-atomic relative to the advertised format. | Andor3, Aravis, and BGAPI2 retain the old feature value, validate before publishing a new layout, restore and verify after failure, and fail closed if rollback cannot be established. | Andor3 mock test forces an unrepresentable post-write layout, observes the original negotiated format, and passes under sanitizers. Aravis and BGAPI2 compile against their SDKs; equivalent fault injection is open. | Andor3 verified; Aravis and BGAPI2 source-reviewed |
| CAMERA-012 | Leave BGAPI2 producer buffers reusable after a failed start. | The shared discard path clears every local `camera_queued` marker after discarding the producer queue. | Source review and SDK build; a start-failure/retry injection test is open. | Implemented; fault injection open |

## Shared-memory interoperability

| ID | Requirement | Implementation | Evidence | Status |
| --- | --- | --- | --- | --- |
| INTEROP-001 | Import and export milk ImageStreamIO streams without making its shared-memory ABI part of PipeWireAO. | Optional upstream-linked `api.imagestreamio.source` and `api.imagestreamio.sink`; ImageStreamIO remains behind an ndarray copy boundary. The source attaches and consumes a claimed semaphore. The sink either creates and owns a new stream or attaches to an exact compatible stream. | Factory test, real ImageStreamIO transport round trip, and direct SPA source/sink test cover format negotiation, initial and updated source publication, sink writes, counters, semaphore wakeup, ownership teardown, and refusal to replace an existing stream. | Functionally verified on CPU-backed rank-one/rank-two arrays and temporal rank-three input; deployment latency qualification open |
| INTEROP-002 | Import and export CPU-backed pyRTC `ImageSHM` streams without embedding Python or NumPy. | `api.pyrtc.source` and `api.pyrtc.sink` map the paired POSIX payload/metadata objects to exact row-major ndarray formats through a copy boundary. Source polling preserves pyRTC count as Header sequence; sink creation and attach modes preserve pyRTC ownership expectations. | Transport and direct SPA tests cover dtype/shape metadata, create collision safety, source initial/update publication, sink writes, counters, ownership teardown, and raw pyRTC-compatible objects. | CPU layout verified synthetically; live pyRTC-process and deployment-latency qualification open; the pyRTC ABI cannot guarantee tear-free concurrent non-blocking reads |

## Scientific graph and structural transforms

The native Calculon SPA factories covered by `CAL-001` through `CAL-012` were
retired on 2026-08-29. Their historical evidence remains under
`docs/benchmark-data`, while maintained scientific implementations and tests
now live in the `calculon-algorithms` FGN bundle.

| ID | Requirement | Implementation | Evidence | Status |
| --- | --- | --- | --- | --- |
| NDARRAY-001 | Reassemble prepared rank-two row blocks without publishing partial frames or changing element values. | `api.ndarray.frame-assembly`, exact schema/profile/rate configuration, preallocated workspace, and strict Header sequence/offset/marker checks. | C integration covers row-major F32 assembly, malformed-gap recovery, column-major U16 assembly, and complete-frame shared/copy passthrough; Rust tests cover construction vocabulary. | Verified for tested formats and recovery paths |
| NDARRAY-002 | Keep the native Rust SPA boundary independent of scientific-project ownership. | `pipewireao-spa-node` shared by ndarray, Nüvü, and HERMES crates. | Workspace tests cover callback exclusion, panic containment, exact POD formats, choices, schemas, and profiles. | Verified |
| NDARRAY-003 | Bridge packed complete-frame camera video into exact ndarray FGN input without interpreting pixels. | `api.ndarray.video-view` for `GRAY8`/U8 and `GRAY16_LE`/U16 with exact size, rate, schema, and optional profile. | C DSO integration verifies format projection, Header preservation, copied storage, and shared-storage forwarding. | Verified synthetically |
| NUVU-001 | Keep HNü240 carrier decoding downstream of camera acquisition and separate from generic transforms. | Nüvü-owned `api.hnu240.decoder` DSO and exact `hnu240-cl-full-8x8-v1` construction profile; pipeline and overscan content are discarded before active detector pixels are published. | Rust mapping tests cover every tap and prove that populated overscan values are absent; C DSO test verifies factory admission and exact 1408 by 131 input and 240 by 240 output video formats. | Complete-frame transform verified synthetically; Aravis carrier-row-block to detector-readout-block conversion and camera hardware remain open |
| FGN-MIGRATION-001 | Use FGN for Calculon scientific operations and avoid a duplicate native SPA implementation. | Native pixel calibration, SHWFS controller, and their full-system harness were removed; the reference graph points to `libcalculon-fgn.so`. | Repository inspection plus Calculon FGN bundle and C-host integration tests in the companion repository. | Implemented; deployment graph qualification remains open |
