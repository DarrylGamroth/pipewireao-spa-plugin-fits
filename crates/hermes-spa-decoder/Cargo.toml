[package]
name = "hermes-spa-decoder"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "PipeWireAO SPA decoder for MPD HERMES FrontPanel batches"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
calculon-spa-node = { path = "../calculon-spa-node" }
libc.workspace = true

[lints.rust]
missing_docs = "warn"
unsafe_code = "warn"
